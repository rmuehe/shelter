<% if user_signed_in? %>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%# <a class="navbar-brand" href="#">Shelter</a> %>
    <%= link_to 'Shelter', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%# <a class="nav-link" href="#">Link</a> %>
          <%= link_to "Reservations", user_path(current_user), class: "nav-link" %>
        </li>

        <li class="nav-item">
          <%= link_to "Sign Out", destroy_user_session_path,
                      method: :delete,
                      class: "nav-link" %>
          <%# <a class="nav-link" href="#">Link</a> %>
        </li>
      </ul>
    </div>
  </nav>

<% elsif provider_signed_in? %>

  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <%# <a class="navbar-brand" href="#">Shelter</a> %>
    <%= link_to 'Shelter', root_path, class: "navbar-brand" %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <%# <a class="nav-link" href="#">Link</a> %>
          <%= link_to "Reservations", provider_path(current_provider), class: "nav-link" %>
        </li>

        <li class="nav-item">
          <%= link_to "Sign Out", destroy_provider_session_path,
                      method: :delete,
                      class: "nav-link" %>
          <%# <a class="nav-link" href="#">Link</a> %>
        </li>
      </ul>
    </div>
  </nav>

<% else %>

  <nav class="navbar navbar-expand-sm   navbar-dark bg-dark">
    <a class="navbar-brand" href="#">Shelter</a>
    <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
      <li class="nav-item dropdown dmenu">
        <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        Clients
        </a>
        <div class="dropdown-menu sm-menu">
          <%= link_to "Sign In", new_user_session_path, class: "dropdown-item" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "dropdown-item" %>
        </div>
      </li>
      <li class="nav-item dropdown dmenu">
        <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
        Providers
        </a>
        <div class="dropdown-menu sm-menu">
          <%= link_to "Sign In", new_provider_session_path, class: "dropdown-item" %>
          <%= link_to "Sign Up", new_provider_registration_path, class: "dropdown-item" %>
        </div>
      </li>
    </ul>
  </nav>

<% end %>